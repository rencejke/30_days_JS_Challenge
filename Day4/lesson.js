/*

--if

syntax
if (condition) {
  this part of code runs for truthy condition
}

--if else

--syntax
if (condition) {
  this part of code runs for truthy condition
} else {
  this part of code runs for false condition
}

--If Else if Else

syntax
if (condition) {
     code
} else if (condition) {
    code
} else {
     code
}

--switch case
Switch is an alternative for if else if else else. 
The switch statement starts with a switch keyword followed 
by a parenthesis and code block. Inside the code block we will have different cases. 

let weather = 'cloudy'
switch (weather) {
  case 'rainy':
    console.log('You need a rain coat.')
    break
  case 'cloudy':
    console.log('It might be cold, you need a jacket.')
    break
  case 'sunny':
    console.log('Go out freely.')
    break
  default:
    console.log(' No need for rain coat.')
}


*/

